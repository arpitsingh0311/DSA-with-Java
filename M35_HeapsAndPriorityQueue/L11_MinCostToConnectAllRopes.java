public class L11_MinCostToConnectAllRopes {
    public static void main(String[] args) {
         

// Ques-9-) GFG Minimum Cost to Connect all ropes.

/*
class Solution {
    // Function to return the minimum cost of connecting the ropes.
    public long minCost(long[] arr) {
        PriorityQueue<Long> pq = new PriorityQueue<>();
        for(int i=0;i<arr.length;i++){
            pq.add(arr[i]);
        }
        long ans = 0;
        while(pq.size()>1){
            long min1 = pq.remove(); 
            long min2 = pq.remove();
            long a = min1 + min2;
            pq.add(a);
            ans+=a;
        }
        return ans;
    }
}
*/










    }
}
